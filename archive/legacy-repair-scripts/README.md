# Legacy Repair Scripts

This folder contains one-off repair scripts that previously lived at the repository root.

They are retained for historical reference only. Do not run them as part of the normal CapturePilot workflow unless you first inspect the target paths and verify the script still matches the current codebase.

Current operational tools live in `tools/`.
